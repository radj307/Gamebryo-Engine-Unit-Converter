/* globals.h contains all the global data used throughout the program
 **/
#pragma once
#include <iostream>

// shortcut for double type
typedef double d; 
// conversion ratio	((m) / (u))
const d __cv = 0.01428222656;